{% extends '::base.html.twig' %}

{% block body -%}
    <h1>HomeData list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Description</th>
                <th>Img</th>
                <th>Url</th>
                <th>Lang</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('homedata_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.img }}</td>
                <td>{{ entity.url }}</td>
                <td>{{ entity.lang }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('homedata_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('homedata_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('homedata_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
